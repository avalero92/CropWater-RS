# Sample data

These files are synthetic and only demonstrate the expected structure.

- `parcels_example.gpkg`: four artificial polygons with `ID` and `CROPS`.
- `meteorology_example.csv`: illustrative daily `Date`, `ETo` and `Pe`.

Do not use the sample values for scientific interpretation.
