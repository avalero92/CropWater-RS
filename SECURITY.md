# Security

CropWater-RS requires user-owned CDSE OAuth credentials.

- Never commit Client Secrets.
- Never include credentials in Zenodo deposits.
- Rotate credentials immediately if accidentally exposed.
- No production credentials are bundled with this release.
